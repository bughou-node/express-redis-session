var redis_session = require('../index.js');

var assert = require("assert");


describe('redis_session', function () {

  it('no params', function() {
    var process = redis_session(redis);

    assert( date_time_reg.test(s) );
  });

});
